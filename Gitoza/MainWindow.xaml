<Window x:Class="Gitoza.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Gitoza"
        Title="Local Repo Punch Card" Height="350" Width="525">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label>Project local folder:</Label>
            <TextBox Margin="10,0" Grid.Column="1" Text="{Binding Path=Path}"></TextBox>
            <Button Grid.Column="2" Command="{Binding Refresh}">Refresh</Button>
        </Grid>
        <Grid Grid.Row="1">
            <local:Week />
        </Grid>
    </Grid>
</Window>
